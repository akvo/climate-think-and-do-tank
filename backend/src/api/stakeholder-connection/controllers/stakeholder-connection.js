'use strict';

/**
 * stakeholder-connection controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::stakeholder-connection.stakeholder-connection');
